part of 'pages.dart';

class ShowCameraPage extends StatelessWidget {
  const ShowCameraPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
